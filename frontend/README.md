# YouthPortal Frontend

This is a Vite + React frontend for the YouthPortal project.

## Setup

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`

## Environment

Set `VITE_API_URL` to point to the backend API (default `http://localhost:5000/api`).
